# AGENTS.md

## Cursor Cloud specific instructions

This repository (`roleradar`) is currently an empty scaffold — it contains only a `README.md` with the project name and no source code, dependencies, or build configuration.

### Current state

- **No source code, package manifests, or build tools exist yet.**
- No services to start, no tests to run, no linter configured.
- The update script is a no-op (`echo "No dependencies to install"`).

### When code is added

Once the project has source files and a package manager, update the `SetupVmEnvironment` update script to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`, etc.) and revise this section with service startup and testing instructions.
